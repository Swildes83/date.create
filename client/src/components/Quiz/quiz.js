import React from "react";
import "./Quiz.css";

const Quiz = (props) => (
    <div 
        className="col-md-4" 
        // style={{backgroundImage: `url("${props.image}")`}}
        // onClick= {() => props.updateScore(props)}
    >
    </div>
);

export default Quiz;